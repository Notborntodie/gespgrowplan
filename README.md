# 信奥成长计划 - GESP 练习系统

信奥成长计划是一个专为信息学奥林匹克竞赛设计的综合性练习平台，支持 GESP（全国青少年编程能力等级考试）和 SmartOJ（智能在线判题系统）。系统提供在线练习、考试管理、自动判题、学习计划管理和成绩分析等功能。

## 📋 项目概述

本项目是一个完整的在线判题系统（OJ - Online Judge），主要面向 GESP 考试练习，同时支持通用的编程题目练习。系统采用前后端分离架构，包含前端应用、后端 API 服务和 AI 处理服务三个核心模块。

### 核心功能

- 🎯 **多级考试系统**: 支持不同等级的 GESP 考试，包括客观题和编程题
- 💻 **在线判题系统（OJ）**: 支持 C++ 等语言的代码提交和自动判题
- 📚 **学习计划管理**: 创建学习计划、分配任务、跟踪学习进度
- 📝 **题目管理**: 支持文本题、代码题，支持批量上传和 AI 智能提取
- 👥 **用户管理**: 多角色支持（学生、教师、管理员），基于 RBAC 的权限控制
- 📊 **成绩分析**: 错题统计、知识点掌握情况分析、学习进度跟踪
- 🤖 **AI 智能服务**: PDF 题目智能提取、题目解析生成
- 🖼️ **图片上传**: 支持题目图片上传和管理
- 📱 **响应式设计**: 适配不同设备屏幕

## 📁 项目结构

```
gesp-practice-system/
├── frontend/                  # 前端 Vue.js 应用（信奥成长计划）
│   ├── src/
│   │   ├── components/       # Vue 组件
│   │   │   ├── admin/        # 管理员组件
│   │   │   ├── teacher/      # 教师组件
│   │   │   └── Dialog/        # 对话框组件
│   │   ├── views/            # 页面视图
│   │   ├── router/           # 路由配置
│   │   ├── stores/           # Pinia 状态管理
│   │   ├── services/         # 服务层
│   │   └── config/           # 配置文件
│   ├── package.json
│   └── vite.config.ts
│
├── backend_server/            # 后端 Node.js 服务器
│   ├── app.js                # Express 应用主文件
│   ├── server.js             # 服务器启动入口
│   ├── config/               # 配置文件
│   │   ├── database.js       # MySQL 连接池配置
│   │   ├── cache.js          # Redis 缓存配置
│   │   └── logger.js         # Winston 日志配置
│   ├── middleware/           # 中间件
│   │   ├── security.js       # 安全中间件
│   │   └── rateLimit.js      # 限流中间件
│   ├── routes/               # 路由
│   │   ├── auth.js           # 认证路由
│   │   ├── users.js          # 用户路由
│   │   ├── exams.js          # 考试路由
│   │   ├── questions.js      # 题目路由
│   │   ├── oj.js             # 在线判题路由
│   │   ├── submissions.js    # 提交记录路由
│   │   ├── knowledge.js      # 知识点路由
│   │   ├── learningPlans.js  # 学习计划路由
│   │   └── teachers.js       # 教师路由
│   ├── services/             # 服务层
│   │   ├── judgeQueue.js     # 判题队列服务
│   │   ├── isolateJudge.js   # 代码判题服务（isolate 沙箱）
│   │   └── containerPool.js  # 容器池管理（已弃用）
│   ├── database/             # 数据库相关
│   │   ├── *.sql             # 数据库备份/初始化文件
│   │   └── import_database.sh # 数据库导入脚本
│   ├── isolate-master/       # isolate 沙箱源码
│   ├── uploads/              # 上传文件目录
│   ├── oj_data/              # OJ 题目数据目录
│   ├── logs/                 # 日志目录
│   ├── docs/                 # 文档目录
│   ├── package.json
│   └── ecosystem.config.js   # PM2 配置文件
│
├── Al_server/                # AI 处理服务（Python FastAPI）
│   ├── main.py               # FastAPI 应用入口
│   ├── explanation_processor.py  # 题目解析处理器
│   ├── llm_processor.py      # 大模型处理器
│   ├── llm_stream_processor.py   # 流式处理
│   ├── requirements.txt      # Python 依赖
│   ├── start.sh              # 启动脚本
│   └── README.md             # AI 服务文档
│
└── README.md                 # 本文件
```

## 🛠️ 技术栈

### 前端技术栈

- **核心框架**: Vue 3 + TypeScript
- **构建工具**: Vite
- **路由管理**: Vue Router 4
- **状态管理**: Pinia
- **代码编辑器**: CodeMirror 6（支持 C++ 语法高亮）
- **UI 组件**: 自定义组件 + Heroicons
- **数学公式**: KaTeX
- **Markdown 渲染**: markdown-it + marked
- **HTTP 客户端**: Axios
- **文件处理**: file-saver, docx

### 后端技术栈

- **运行环境**: Node.js 18+
- **Web 框架**: Express 4.x
- **数据库**: MySQL 8.0+ (使用 mysql2 驱动)
- **缓存/队列**: Redis 5.0+ (使用 ioredis)
- **任务队列**: Bull (基于 Redis)
- **代码沙箱**: isolate (IOI 官方隔离执行环境)
- **认证**: JWT (jsonwebtoken)
- **密码加密**: bcryptjs
- **文件上传**: Multer
- **数据验证**: Joi
- **日志**: Winston
- **进程管理**: PM2
- **安全**: Helmet, CORS, express-rate-limit

### AI 服务技术栈

- **Web 框架**: FastAPI (Python)
- **PDF 处理**: pdfplumber
- **大模型 API**: 阿里云 DashScope (Qwen 系列)
- **并发处理**: ThreadPoolExecutor
- **服务器**: Uvicorn

### 数据库

- **数据库**: MySQL 8.0+
- **字符集**: utf8mb4
- **排序规则**: utf8mb4_unicode_ci
- **连接池**: 支持连接池管理，默认 20 个连接

## 🚀 快速开始

### 环境要求

- **Node.js**: >= 18.0.0 (推荐 LTS 版本)
- **Python**: >= 3.6 (用于 AI 服务)
- **MySQL**: >= 8.0
- **Redis**: >= 5.0
- **npm**: >= 9.0.0 (或使用 yarn/pnpm)
- **isolate**: 需要安装并配置（用于代码沙箱执行）

### 安装步骤

#### 1. 克隆项目

```bash
git clone <repository-url>
cd gesp-practice-system
```

#### 2. 安装后端依赖

```bash
cd backend_server
npm install
```

#### 3. 安装前端依赖

```bash
cd ../frontend
npm install
```

#### 4. 安装 AI 服务依赖

```bash
cd ../Al_server
pip install -r requirements.txt
```

#### 5. 配置数据库

```bash
# 创建数据库
mysql -u root -p
CREATE DATABASE gesp_practice_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;

# 导入数据库结构（使用数据库目录下的 SQL 文件）
cd backend_server/database
mysql -u root -p gesp_practice_system < gesp_practice_system_dump_*.sql
# 或者使用导入脚本
./import_database.sh
```

#### 6. 配置环境变量

**后端环境变量** (`backend_server/.env`):

```bash
cd backend_server
cp env.example .env
# 编辑 .env 文件，配置以下内容：
```

```env
# 应用配置
NODE_ENV=development
PORT=3000

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_NAME=gesp_practice_system
DB_USER=your_username
DB_PASSWORD=your_password
DB_CONNECTION_LIMIT=20

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# 文件上传配置
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760
```

**前端环境变量** (`frontend/.env`):

```bash
cd frontend
# 创建 .env 文件
```

```env
# API 基础 URL
VITE_API_BASE_URL=http://localhost:3000/api

# AI 服务 API 基础 URL（可选）
VITE_AI_API_BASE_URL=http://localhost:8000/api

# 开发环境模式
VITE_MODE=development
```

**AI 服务环境变量** (`Al_server/.env`):

```bash
cd Al_server
# 创建 .env 文件
```

```env
# 必需配置
DASHSCOPE_API_KEY=sk-你的实际API密钥
LLM_MODEL=qwen-plus-latest
```

#### 7. 安装和配置 isolate 沙箱

```bash
cd backend_server/isolate-master
make isolate
sudo make install
sudo isolate --init
```

#### 8. 启动服务

**启动后端服务**:

```bash
cd backend_server
# 开发模式（使用 nodemon，自动重启）
npm run dev

# 生产模式
npm start

# 或使用 PM2
npm run pm2:start
```

**启动前端服务**:

```bash
cd frontend
npm run dev
```

访问 `http://localhost:5173` 即可看到前端应用。

**启动 AI 服务**（可选）:

```bash
cd Al_server
./start.sh
# 或
python main.py
```

访问 `http://localhost:8000` 查看 AI 服务文档。

## 📚 功能模块

### 1. 用户认证与授权

- 用户注册、登录、登出
- JWT Token 认证
- 基于角色的访问控制（RBAC）
- 支持学生、教师、管理员三种角色

### 2. 客观题系统

- 考试创建和管理
- 题目管理（单选、多选、判断）
- 答题记录和成绩统计
- 错题收集和分析
- 支持题目图片上传

### 3. 在线判题系统（OJ）

- 编程题目管理
- 代码在线编辑（CodeMirror）
- 支持 C++ 语言
- 自动判题（使用 isolate 沙箱）
- 测试用例管理
- 提交历史记录
- 实时判题结果反馈

### 4. 学习计划系统

- 学习计划创建和管理
- 任务分配（考试任务、OJ 任务）
- 学习进度跟踪
- 任务完成统计
- 支持教师分配计划给学生

### 5. 知识点管理

- 知识点分类和层级管理
- 题目与知识点关联
- 知识点掌握情况分析
- 按知识点筛选题目

### 6. 教师功能

- 学生管理（查看学生列表、添加学生）
- 成绩查询（客观题成绩、OJ 提交记录）
- 学习进度监控
- 学习计划管理

### 7. 统计分析

- 答题统计（正确率、错题统计）
- 知识点掌握情况分析
- 学习进度可视化
- 成绩趋势分析

### 8. AI 智能服务

- PDF 题目智能提取（使用大模型 API）
- 题目解析自动生成
- 批量题目处理
- 流式处理支持

## 📖 文档

### API 文档

- [项目架构文档](./backend_server/docs/项目架构文档.md) - 详细的后端架构说明
- [数据库文档](./backend_server/docs/数据库.md) - 数据库设计文档
- [任务进度相关接口说明](./backend_server/docs/任务进度相关接口说明.md) - 学习计划相关接口

### 其他文档

- [前端 README](./frontend/README.md) - 前端项目说明
- [后端 README](./backend_server/README.md) - 后端项目说明
- [AI 服务 README](./Al_server/README.md) - AI 服务说明

## 🔧 开发指南

### 开发环境设置

1. 按照"快速开始"部分安装和配置所有依赖
2. 确保 MySQL、Redis 服务正在运行
3. 启动后端服务：`cd backend_server && npm run dev`
4. 启动前端服务：`cd frontend && npm run dev`
5. 启动 AI 服务（可选）：`cd Al_server && ./start.sh`

### 代码规范

- **前端**: 使用 TypeScript，遵循 Vue 3 Composition API 规范
- **后端**: 使用 ES6+ 语法，遵循 Express 最佳实践
- **命名规范**: 
  - 变量/函数：驼峰命名（camelCase）
  - 常量：大写+下划线（UPPER_SNAKE_CASE）
  - 文件名：小写+下划线（snake_case）

### 测试

```bash
# 后端 API 测试
cd backend_server
npm test

# 前端单元测试
cd frontend
npm run test:unit

# 前端 E2E 测试
npm run test:e2e
```

## 🚢 部署

### 生产环境部署

详细的部署指南请参考：

- [后端部署脚本](./backend_server/deploy.sh)
- [前端部署脚本](./frontend/deploy-frontend.sh)
- [PM2 配置](./backend_server/ecosystem.config.js)

### 部署要点

1. **环境变量**: 确保生产环境的 `.env` 文件配置正确
2. **数据库**: 使用生产数据库，确保备份机制
3. **Redis**: 确保 Redis 服务正常运行
4. **isolate**: 确保 isolate 已正确安装和配置
5. **PM2**: 使用 PM2 管理 Node.js 进程
6. **Nginx**: 配置反向代理和静态文件服务
7. **SSL**: 配置 HTTPS 证书

## 🔒 安全机制

- **认证**: JWT Token 认证
- **授权**: 基于角色的访问控制（RBAC）
- **密码加密**: bcryptjs 加密存储
- **SQL 注入防护**: 使用参数化查询
- **XSS 防护**: 请求数据清理和响应头设置
- **请求限流**: 基于 Redis 的限流机制
- **文件上传**: 文件类型和大小验证
- **代码沙箱**: 使用 isolate 隔离执行用户代码

## 📊 性能优化

- **连接池**: MySQL 连接池管理（默认 20 个连接）
- **Redis 缓存**: 热点数据缓存（题目信息、测试样例等）
- **异步处理**: 判题任务使用 Bull 队列异步处理
- **负载均衡**: PM2 集群模式，多进程处理请求
- **代码沙箱**: 使用 isolate 替代 Docker，性能提升 10-20 倍

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开 Pull Request

## 📝 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情
