# GESP前端环境变量配置示例
# 复制此文件为 .env 并根据实际情况修改配置

# API基础URL（后端服务地址）
# 开发环境示例: http://localhost:3000/api
VITE_API_BASE_URL=http://localhost:3000/api

# AI服务API基础URL（如果有单独的AI服务）
# 开发环境示例: http://localhost:8000/api
VITE_AI_API_BASE_URL=http://localhost:8000/api

# 判题机 API 配置（JSON 格式，支持多个判题机实现负载均衡）
# 格式：JSON 数组，每个元素包含 url、name、priority、enabled 字段
# - url: 判题机 API 地址（必需）
# - name: 判题机名称（可选，默认：判题机 N）
# - priority: 优先级，数字越小优先级越高（可选，默认：按顺序）
# - enabled: 是否启用（可选，默认：true）
#
# 示例1：单个判题机（使用主 API 服务器）
# VITE_OJ_API_CONFIGS='[{"url":"http://localhost:3000","name":"本地判题机","priority":1,"enabled":true}]'
#
# 示例2：多个判题机（负载均衡）
# VITE_OJ_API_CONFIGS='[{"url":"http://localhost:3000","name":"主判题机","priority":1,"enabled":true},{"url":"http://localhost:8000","name":"备用判题机","priority":2,"enabled":true}]'
#
# 如果不配置，将使用 VITE_API_BASE_URL 作为默认判题机
# VITE_OJ_API_CONFIGS=
