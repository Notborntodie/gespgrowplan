# 部署配置文件示例
# 复制此文件为 .deploy-config 并根据实际情况修改配置

# 服务器配置
DEPLOY_SERVER_IP=106.14.143.27
DEPLOY_SERVER_USER=root
DEPLOY_PATH=/var/www/gesp-frontend
NGINX_CONFIG_PATH=/etc/nginx/conf.d/gesp-frontend.conf

# 域名配置（用于HTTPS和server_name，如果使用IP访问则填写IP地址）
DOMAIN_NAME=106.14.143.27

# HTTPS/SSL配置（可选，如果配置了SSL证书路径，则自动启用HTTPS）
# 如果不配置以下两个路径，则只使用HTTP（80端口）
#
# 使用自签名证书（仅IP地址，浏览器会显示警告）：
# 1. 运行: ./generate-self-signed-cert.sh 生成证书
# 2. 取消下面两行的注释并配置路径：
#SSL_CERT_PATH=/etc/ssl/gesp-self-signed/server.crt
#SSL_KEY_PATH=/etc/ssl/gesp-self-signed/server.key
#
# 使用Let's Encrypt证书（需要域名）：
# SSL_CERT_PATH=/etc/letsencrypt/live/your-domain.com/fullchain.pem
# SSL_KEY_PATH=/etc/letsencrypt/live/your-domain.com/privkey.pem
#
# 如果不启用HTTPS，保持以下两行为空：
# SSL_CERT_PATH=
# SSL_KEY_PATH=
# ENABLE_HTTPS=false

# API配置（用于构建时注入环境变量）
# 注意：如果启用了HTTPS，建议API地址也使用HTTPS或配置Nginx反向代理
API_BASE_URL=http://106.14.143.27:3000/api
AI_API_BASE_URL=http://106.14.143.27:8000/api

# 判题机 API 配置（JSON 格式，支持多个判题机实现负载均衡）
# 格式：JSON 数组，每个元素包含 url、name、priority、enabled 字段
# - url: 判题机 API 地址（必需）
# - name: 判题机名称（可选，默认：判题机 N）
# - priority: 优先级，数字越小优先级越高（可选，默认：按顺序）
# - enabled: 是否启用（可选，默认：true）
#
# 示例1：单个判题机
# OJ_API_CONFIGS='[{"url":"http://106.14.143.27:3000","name":"主判题机","priority":1,"enabled":true}]'
#
# 示例2：多个判题机（负载均衡）
OJ_API_CONFIGS='[{"url":"http://121.4.103.140:8000","name":"主服务器","priority":1,"enabled":true},{"url":"http://106.14.143.27:3000","name":"备用服务器","priority":2,"enabled":true}]'
#
# 如果不配置，将使用 API_BASE_URL 作为默认判题机
#OJ_API_CONFIGS=

